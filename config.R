

search_terms <- list("keywords" = 'data%2Bvisualization', 
                     "location" = "ireland", #main location for Linkedin
                     "location2" = "dublin", #E.g. smaller more relevant location
                     "geoId" = "", #will be needed at some point
                     'trk' = "",  #no idea what this is yet
                     "start" = 0, #LinkedIn is zero-indexed
                     "end" = 2000,  #0-24, 25-49,etc. This is the last page to include in job search
                     "steps" = 25) #default 25 jobs listed per page              
